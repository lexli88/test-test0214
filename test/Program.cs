using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using System.Text.RegularExpressions;
using System.Threading.Tasks;

namespace test
{
    class Program
    {
        static void Main(string[] args)
        {
            //邮政编码与身份证号码正则表达式
            while (true)
            {
                //Console.WriteLine("请输入一个邮政编码");
                //string postcode = Console.ReadLine();

                //string reg = @"^\d{6}$";
                //Console.WriteLine(Regex.IsMatch(postcode, reg));

                //Console.WriteLine("Id card number:");
                //string id = Console.ReadLine();
                //string reg = @"^(\d{15}|\d{18})$";
                //Console.WriteLine(Regex.IsMatch(id, reg));

            }
        }
    }
}
